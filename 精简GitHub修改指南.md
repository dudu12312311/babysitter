# 精简 GitHub 修改指南

## 🎯 只需修改这3个文件

访问：https://github.com/dudu12312311/babysitter

---

## ✅ 第1步：修改 nixpacks.toml

1. 点击 `nixpacks.toml` 文件
2. 点击铅笔图标（编辑）
3. **查看当前内容**，如果不是下面这样，就全选删除并替换：

```toml
[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "python main.py"
```

4. 提交：`修复启动命令` → Commit changes

---

## ✅ 第2步：修改 railway.json

1. 点击 `railway.json` 文件
2. 点击铅笔图标
3. **查看当前内容**，如果不是下面这样，就全选删除并替换：

```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "python main.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
```

4. 提交：`更新railway配置` → Commit changes

---

## ✅ 第3步：确认 requirements.txt

1. 点击 `requirements.txt` 文件
2. **查看内容**，应该只有：

```
Flask==2.3.3
```

3. 如果有其他内容（比如 gunicorn），删除它们，只保留 Flask
4. 如果修改了，提交更改

---

## ✅ 第4步：确认 Procfile

1. 点击 `Procfile` 文件
2. **查看内容**，应该是：

```
web: python main.py
```

3. 如果不是，修改它
4. 提交更改

---

## 🔍 第5步：检查是否有其他启动文件

在 GitHub 仓库根目录查找：
- ❌ 如果有 `start.sh` → 删除它
- ❌ 如果有 `run.sh` → 删除它
- ❌ 如果有 `startup.sh` → 删除它
- ❌ 如果 requirements.txt 里有 `uvicorn` 或 `gunicorn` → 删除这些行

---

## ⏳ 第6步：等待 Railway 重新部署

1. 修改完成后，回到 Railway 控制台
2. 等待 1-2 分钟
3. Railway 会自动检测到 GitHub 更新并重新部署
4. 查看部署日志

---

## ✅ 第7步：验证

访问：`https://你的应用.railway.app/health`

应该看到：`{"status": "healthy"}`

---

## 🆘 如果还是失败

**告诉我：**
1. GitHub 上这4个文件的内容是什么？（截图或复制）
2. Railway 部署日志显示什么错误？（截图）
3. 是否还有其他 .sh 或启动脚本文件？

我会根据实际情况给你精确的解决方案！
