# 🎨 中国宝宝照片生成 - 快速参考卡

## 🚀 3步开始

```bash
# 1. 安装依赖
pip install fal-client

# 2. 设置API密钥
set FAL_KEY=你的密钥

# 3. 运行测试
python test_baby_photo.py
```

## 📝 代码示例

### 基础用法
```python
from baby_photo_integration import BabyPhotoGenerator

generator = BabyPhotoGenerator()
result = generator.generate_baby_photo(
    age_months=6,
    gender="boy",
    expression="happy",
    scene="studio"
)

if result["success"]:
    print(result["image_url"])
```

### 集成到游戏
```python
# 在游戏开始时
result = generator.generate_for_game_state(game_state)
```

### API 调用
```bash
curl -X POST http://localhost:5000/api/baby-photo/generate \
  -H "Content-Type: application/json" \
  -d "{\"age_months\": 6, \"gender\": \"boy\"}"
```

## 🎯 参数速查

| 参数 | 类型 | 选项 |
|------|------|------|
| age_months | int | 0-36 |
| gender | str | "boy", "girl", None |
| expression | str | "happy", "sleeping", "curious", "crying", "neutral" |
| scene | str | "studio", "home", "outdoor" |

## 📊 年龄映射

| 月龄 | 阶段 | 特征 |
|------|------|------|
| 0-3 | newborn_0_3 | 新生儿，柔软皮肤 |
| 3-12 | infant_3_12 | 婴儿，开始互动 |
| 1-2岁 | toddler_1_2 | 幼儿，活泼好动 |
| 2-3岁 | preschool_2_3 | 学龄前，表达清晰 |

## 🔑 核心提示词

### 正面（强化）
- `(pure Chinese baby:1.5)` - 纯中国血统
- `(single eyelid:1.2)` - 单眼皮
- `(black straight hair:1.3)` - 黑直发
- `(dark brown eyes:1.3)` - 深棕眼
- `(flat nose bridge:1.1)` - 扁鼻梁

### 负面（排除）
- `mixed race, caucasian` - 混血、白人
- `blonde hair, blue eyes` - 金发、蓝眼
- `(double eyelids:1.3)` - 双眼皮
- `curly hair` - 卷发

## 🛠️ 修改 main.py

```python
# 1. 导入
from baby_photo_api import baby_photo_bp

# 2. 注册
app.register_blueprint(baby_photo_bp)

# 3. 完成！
```

## 🧪 测试命令

```bash
# 健康检查
curl http://localhost:5000/api/baby-photo/health

# 预览提示词（不消耗配额）
curl -X POST http://localhost:5000/api/baby-photo/preview-prompt \
  -H "Content-Type: application/json" \
  -d "{\"age_months\": 6}"

# 生成照片
curl -X POST http://localhost:5000/api/baby-photo/generate \
  -H "Content-Type: application/json" \
  -d "{\"age_months\": 6, \"gender\": \"boy\"}"
```

## ⚠️ 常见错误

| 错误 | 原因 | 解决 |
|------|------|------|
| ImportError | 未安装 fal-client | `pip install fal-client` |
| 照片功能不可用 | 未设置 FAL_KEY | `set FAL_KEY=密钥` |
| 401 Unauthorized | API密钥无效 | 检查密钥是否正确 |
| 不是中国宝宝 | 提示词权重不够 | 增加权重到 1.8 |

## 📁 文件清单

✅ `chinese_baby_prompts.py` - 提示词配置  
✅ `baby_photo_integration.py` - 集成模块  
✅ `baby_photo_api.py` - API端点  
✅ `test_baby_photo.py` - 测试脚本  
✅ `requirements.txt` - 已更新依赖  

## 📖 详细文档

- `宝宝照片功能总结.md` - 完整总结
- `宝宝照片集成指南.md` - 集成指南
- `修改main.py指南.md` - 修改步骤
- `中国宝宝照片生成指南.md` - 提示词说明

---

**保存此卡片，随时查阅！** 📌
