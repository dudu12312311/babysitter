[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'", "ls -la", "python -c 'import sys; print(sys.version)'"]

[start]
cmd = "python main.py"